<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/testFragment"
        android:name="com.truta.traveljournal.view.TestFragment"
        android:label="fragment_test"
        tools:layout="@layout/fragment_test" />
    <fragment
        android:id="@+id/homeFragment"
        android:name="com.truta.traveljournal.view.HomeFragment"
        android:label="fragment_home"
        tools:layout="@layout/fragment_home" >
        <action
            android:id="@+id/action_homeFragment_to_addMemoryActivity"
            app:destination="@id/addMemoryActivity" />
    </fragment>
    <activity
        android:id="@+id/addMemoryActivity"
        android:name="com.truta.traveljournal.view.AddMemoryActivity"
        android:label="activity_add_memory"
        tools:layout="@layout/activity_add_memory" />
</navigation>